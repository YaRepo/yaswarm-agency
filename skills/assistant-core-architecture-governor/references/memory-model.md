# Memory Model Policy

- Markdown is source of truth.
- SQLite index is derivative and rebuildable.
- Keep daily logs in `memory/YYYY-MM-DD.md`.
- Keep high-level anchor in `MEMORY.md`.
- Never allow hidden memory state with no file trace.
