# Log Schema (CSV)

Required columns:
- match_id
- winner
- loser
- winner_cards (pipe-separated card IDs)
- loser_cards (pipe-separated card IDs)
- turns (optional)

Example winner_cards field:
- IP-001|IP-010|IP-021
